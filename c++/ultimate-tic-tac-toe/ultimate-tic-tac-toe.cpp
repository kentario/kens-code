#include <iostream>
#include <random>
#include <exception>

#include "game.hpp"
#include "bots.hpp"
#include "simulate.hpp"

static std::random_device rng {};

int main () {
  try {

    
  } catch (const std::exception &e) {
    std::cerr << e.what();
    
    return EXIT_FAILURE;
  }
  
  return 0;
}
